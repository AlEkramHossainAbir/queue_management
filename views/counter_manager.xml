<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="create_counter_manager_form_view" model="ir.ui.view">
        <field name="name">Create Counter Manager</field>
        <field name="model">counter.manager</field>
        <field name="arch" type="xml">
            <form string="Create Category">
                <sheet>
                    <group>
                        <field name="counter_manager_name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="create_counter_manager_tree_view" model="ir.ui.view">
        <field name="name">Create Counter Manager</field>
        <field name="model">counter.manager</field>
        <field name="arch" type="xml">
            <tree string="Create Category" editable="bottom">
                <field name="counter_manager_name"/>
            </tree>
        </field>
    </record>


    <record id="create_counter_manager_action" model="ir.actions.act_window">
        <field name="name">Create Counter Manager</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">counter.manager</field>
        <field name="view_mode">tree</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Add Text Here
            </p>
            <p>
                More details about what a user can do with this object will be OK
            </p>
        </field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, That's why It needs NO parent or action -->
    <menuitem id="create_counter_manager" name="Create Counter Manager" sequence="103"
              parent="queue_management_root"
              action="create_counter_manager_action"/>
</odoo>