<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="report_generate_tree_view" model="ir.ui.view">
        <field name="name">Create counter</field>
        <field name="model">queue.report</field>
        <field name="arch" type="xml">
            <tree string="Report" editable="bottom">
                <field name="token_no"/>
                <field name="counter_no"/>
                <field name="datetime"/>
                <field name="engineer"/>
            </tree>
        </field>
    </record>


    <record id="print_report_action" model="ir.actions.act_window">
        <field name="name">Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">queue.report</field>
        <field name="view_mode">tree</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Add Text Here
            </p>
            <p>
                More details about what a user can do with this object will be OK
            </p>
        </field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, That's why It needs NO parent or action -->
    <menuitem id="report_generate" name="Print Report" sequence="106" parent="queue_management_root"
              action="print_report_action"/>
</odoo>