<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="assign_counter_tree_view" model="ir.ui.view">
        <field name="name">Create counter</field>
        <field name="model">queue.officer_assign</field>
        <field name="arch" type="xml">
            <tree string=" Create Token" editable="bottom">
                <field name="counter_num"/>
                <field name="currently_assign"/>
                <field name="current_token"/>
            </tree>
        </field>
    </record>


    <record id="assign_counter_action" model="ir.actions.act_window">
        <field name="name">Create counter</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">queue.officer_assign</field>
        <field name="view_mode">tree</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Add Text Here
            </p>
            <p>
                More details about what a user can do with this object will be OK
            </p>
        </field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, That's why It needs NO parent or action -->
    <menuitem id="assign_to_me" name="Assign Counter" sequence="107" parent="queue_management_root"
              action="assign_counter_action"/>
</odoo>